// import {tizen, WebAPIError, HumanActivityHRMData } from 'tizen.wearable.2.3.2';

tizen.humanactivitymonitor.start("HRM", (hrmData: HumanActivityHRMData) => {
    
} , (e: WebAPIError) => {
    
});

class Foo {
    
}

